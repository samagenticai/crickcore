/**
 * Legacy helper — Pro checkout is now available to guests directly from Pricing.
 * Kept as a no-op so App.jsx import remains stable.
 */
export default function CheckoutResume() {
  return null;
}
