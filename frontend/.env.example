# Development: relative /api uses Vite proxy.
# Production (Vercel monorepo): leave as /api — same origin as the SPA.
VITE_API_URL=/api

# Stripe Test Mode ONLY — use a test publishable key (never pk_live_).
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
